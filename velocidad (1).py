# Programa para calcular la velocidad (v = x / t)

x = float(input("Ingresa la distancia (en km): "))
t = float(input("Ingresa el tiempo (en horas): "))

v = x / t

print("La velocidad es:", v, "km/h")
